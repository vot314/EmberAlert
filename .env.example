# Optional. Without a key the app serves committed fixture extractions and the
# demo runs exactly the same — the key only switches the pipeline to live Gemini.
GEMINI_API_KEY=

# Optional override if the model id in lib/cache.ts has moved on.
# GEMINI_MODEL=gemini-3.5-flash
